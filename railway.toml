[build]
builder = "nixpacks"
buildCommand = "cd HrmsApi && dotnet restore && dotnet build -c Release"

[deploy]
startCommand = "cd HrmsApi && dotnet run --urls=http://0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
